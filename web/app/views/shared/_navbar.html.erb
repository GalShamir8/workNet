<nav class="navbar navbar-default">
  <div class="container-fluid"> 
    <div class="navbar-header">
      <a>
        <%= link_to user_path(current_user) do %>
          <%= image_tag(current_user.profile_pictures.first.variant(resize: "50x50"), class: "rounded") if current_user.profile_pictures.attached? %>
        <% end %>
      </a>
    </div>

    <div class="navbar-brand">
      <a>
        <%= link_to root_path do %>
          <%= image_tag 'home-icon.png', width: "30", height: "30", class: "d-inline-block align-center", alt: "" %>
        <% end %>
    </div>

    <div class="navbar-brand">
      <a>
        <%= link_to users_path do %>
          <i class="expolre-user-icon glyphicon glyphicon-th d-inline-block align-center"></i>
        <% end %>
    </div>

    <div class="collapse navbar-collapse d-flex" id="bs-example-navbar-collapse-1">      
      <div class="container-navbar-right">
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <%= image_tag("settings-icon.png", height: "30") %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">            
              <li><a href=<%= static_pages_privacy_policy_path %> data-toggle="modal" data-target="#privacy-policy-modal">Privacy Policy</a></li>
              <li><a href="#">About Us</a></li>
              <li><a href="#">Help & Support</a></li>
              <li role="separator" class="divider"></li>
              <li><a><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></a></li>
            </ul>
          </li>
        </ul>
        <form class="navbar-form navbar-right">
          <%= form_tag(request.path, method: :get) do %>
            <div class="form-group">
              <%= search_field_tag :query, params[:query], placeholder: "Search", class: "form-control" %>
              <%= button_tag "Search", class: "btn btn-default" %> 
            </div>
          <% end %>            
        </form>
      </div>
    </div>
  </div>
</nav>