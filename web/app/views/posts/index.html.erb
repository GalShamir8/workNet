<p style="color: green"><%= notice %></p>
<h1>Posts</h1>

<div id="posts">
    <p>
      <%= render @posts %>
    </p>
</div>
<%= will_paginate @posts %>

<%= will_paginate @posts %>
<%= link_to "New post", new_post_path %>
