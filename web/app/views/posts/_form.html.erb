<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %><br />
    <%= form.text_area :description, autofocus: true, autocomplete: "Share yout thoughts.." %>
  </div>

  <div class="field">
    <%= form.label :attachments %><br />
    <%= form.file_field :attachments, autofocus: true, multiple: true%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
